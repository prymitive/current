version: "2"
run:
  tests: true
linters:
  enable:
    - contextcheck
    - copyloopvar
    - errname
    - errorlint
    - exhaustive
    - exptostd
    - gocritic
    - godot
    - importas
    - misspell
    - nakedret
    - nilnesserr
    - perfsprint
    - prealloc
    - predeclared
    - promlinter
    - revive
    - testifylint
    - unconvert
    - usetesting
  settings:
    govet:
      enable-all: true
  exclusions:
    generated: lax
    presets:
      - comments
      - std-error-handling
issues:
  max-same-issues: 0
formatters:
  enable:
    - gofumpt
    - goimports
  settings:
    gofumpt:
      extra-rules: true
    goimports:
      local-prefixes:
        - github.com/prymitive/current
